CREATE DATABASE HIS_CORTES
GO
USE HIS_CORTES
GO
/*CREAR TABLA DONDE SE VA ALMACENAR REGISTROS DE TODO EL CORTE DE MES*/
CREATE TABLE CORTE_SEMANAL2 (
	CONSECUTIVO int IDENTITY(1,1) PRIMARY KEY,
	NvecesRepetido INT,
	FECHA_DEL_ESTUDIO DATETIME,
	UNIDAD_TRATANTE varchar(255),
	ApellidoPaterno varchar(255),
	ApellidoMaterno varchar(255),
	NOMBRE_DEL_PACIENTE varchar(255),
	NSS varchar(255),
	CLAVE_CPIM varchar(255),
	AGREGADO_MEDICO varchar(255),
	TIPO_DE_ESTUDIO varchar(255),
	MODALIDAD varchar(255),
	INTERPRETACION VARCHAR(255)
	   		   );
GO